/// make a fnction that adds two number

// args: 2 ints
// returns: an ints
// does:  addition
function adds(a,b) {
	// write the stuff
	return stuff
}

function testAdd(testee) {
	var a = 4;
	var b = 5;
	if (testee(a,b) == 9) {
		return "yay"
	} // else
}

testAdd(adds);